"""
Dashboard — Web UI for controlling This News Now. (Phase B+)

Simple Flask app for tuning tone, topic weights, injecting stories,
and monitoring stream status. Accessible on local network.
"""

# TODO: Implement alongside Phase B
# Endpoints:
#   GET  /            — Dashboard UI
#   POST /dials       — Update tone, topic weights
#   GET  /queue       — See render queue status
#   POST /inject      — Manually inject a story idea
#   GET  /world_bible — View current world state
#   POST /world_bible — Edit world bible entries
